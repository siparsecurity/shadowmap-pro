# Debian build artifacts — generated by dpkg-buildpackage, not source
debian/shadowmap/
debian/.debhelper/
debian/debhelper-build-stamp
debian/files
debian/*.substvars
debian/*.log

# Packaging tarball (was a delivery artifact, not part of the repo)
shadowmap-debian-packaging.tar.gz

# Python
__pycache__/
*.pyc

# Scan output (reports generated by running the tool)
*.html
*.json
!templates/*.json
